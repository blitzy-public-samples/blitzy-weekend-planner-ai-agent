# ============================================================================
# Weekend Planner Frontend - Environment Configuration
# ============================================================================
#
# This file documents the environment variables used by the frontend application.
# Copy this file to .env.local for local development:
#
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local or any .env files containing secrets
# to version control. Only .env.example should be committed.
#
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------

# VITE_API_BASE_URL
# -----------------
# The base URL for the ADK backend API server.
#
# This variable tells the frontend where to send API requests for:
#   - Session creation: POST /apps/{app}/users/{user}/sessions/{session}
#   - Plan generation:  POST /run
#
# Default value: http://localhost:8000
# This assumes the ADK backend is running locally via `adk web` command.
#
# Usage in code:
#   const apiUrl = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000';
#
# Environment-specific values:
#   - Local development:     http://localhost:8000
#   - Docker development:    http://host.docker.internal:8000
#   - Staging environment:   https://staging-api.example.com
#   - Production:            https://api.example.com
#
# Note: All Vite environment variables must be prefixed with VITE_ to be
# exposed to the client-side code. Variables without this prefix are only
# available in the build process.

VITE_API_BASE_URL=http://localhost:8000

# ----------------------------------------------------------------------------
# Alternative Configuration: Using Vite Proxy (Recommended for Development)
# ----------------------------------------------------------------------------
#
# Instead of setting VITE_API_BASE_URL directly, you can use Vite's built-in
# proxy feature to avoid CORS issues during development. This is configured
# in vite.config.ts.
#
# With proxy enabled, you can make requests to /api/* and they will be
# forwarded to the backend server automatically. In this case, you would use:
#
#   VITE_API_BASE_URL=/api
#
# The proxy will rewrite /api/run to http://localhost:8000/run, etc.
# This approach is recommended because:
#   1. Avoids CORS issues entirely during development
#   2. Simulates production routing behavior
#   3. Makes it easier to switch between environments
#
# ----------------------------------------------------------------------------

# ----------------------------------------------------------------------------
# Future Environment Variables (Reserved)
# ----------------------------------------------------------------------------
#
# The following variables may be added in future versions:
#
# VITE_APP_NAME - Application display name
# VITE_API_TIMEOUT - Request timeout in milliseconds (default: 30000)
# VITE_DEBUG_MODE - Enable verbose console logging (default: false)
#
# ----------------------------------------------------------------------------
